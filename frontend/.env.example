# Local development can leave this empty because FastAPI serves the built UI.
# Vercel: set this to the deployed Render API URL, without a trailing slash.
VITE_API_BASE_URL=https://your-api-service.onrender.com
